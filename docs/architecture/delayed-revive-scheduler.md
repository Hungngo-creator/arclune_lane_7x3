# Delayed revive scheduler

Delayed revives are serializable queue records, never timers. Supported clocks are global/ally/enemy natural actions, side passes, global SSI cycles, and target-side opportunities. Due entries are ordered by due value then insertion serial and pass the common revive gateway. Battle end cancels eligibility; future reincarnation may veto through the eligibility result without being implemented here.

