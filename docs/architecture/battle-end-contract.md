# Battle end contract

Battle end is evaluated after the death wave, immediate reactions, and immediate revive. One dead leader loses, simultaneous leader deaths draw, and an immediate revive prevents defeat. Delayed revive never postpones defeat. `BATTLE_ENDED` is idempotent and terminal for SSI actor selection.

