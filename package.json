{
  "name": "arclune_lane_7x3",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run build:prod",
    "build:dev": "node tools/generate-loithienanh-svg.mjs && node build.mjs --mode=development",
    "build:prod": "node tools/generate-loithienanh-svg.mjs && node build.mjs --mode=production",
    "dev": "APP_ENTRY=${APP_ENTRY:-src/main.js} tsx watch $APP_ENTRY",
    "start": "NODE_ENV=${NODE_ENV:-production} APP_ENTRY=${APP_ENTRY:-src/main.js} tsx $APP_ENTRY",
    "test": "jest --runInBand",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "esbuild": "file:tools/esbuild-stub",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "moduleFileExtensions": [
      "ts",
      "js",
      "json",
      "node"
    ],
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    }
  }
}
